// Login button

function login(element) {
    console.log(element);
    element.innerText = "Logout";
}

// Remove Definition when clicked

function removeElement(element) {
    console.log("remove the Add Definition when is clicked");
    element.remove();
}
